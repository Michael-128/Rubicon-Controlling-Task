<div>
    <h2>Najczęściej występujące źródła klientów w zależności od statusu</h2>
    <ul>
        {% for source in topSourcesByStatus %}
        <li> <strong>{{ source.status }}</strong>: {{ source.sources }}</li>
        {% endfor %}
    </ul>
</div>  